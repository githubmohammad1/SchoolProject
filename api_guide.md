# دليل استخدام واجهات برمجة التطبيقات (API Guide)

هذا الدليل يوضح جميع نقاط النهاية (Endpoints) المتاحة في تطبيق `core`، والدور المطلوب للوصول إليها، والغرض من استخدام كل منها في الواجهة الأمامية.

---

## 🔑 I. المصادقة والملف الشخصي (Authentication & Profile)

| المسار (Endpoint) | الطريقة (Method) | الدور المطلوب | الوصف |
| :---: | :---: | :---: | :---: |
| `/api/core/register/` | `POST` | `AllowAny` | إنشاء مستخدم جديد (طالب، مدرس، ولي أمر) وملف شخصي مرتبط به. |
| `/api/auth/jwt/create/` | `POST` | `AllowAny` | **تسجيل الدخول:** تبادل اسم المستخدم وكلمة المرور للحصول على رمز JWT (AccessToken). |
| `/api/core/profile/` | `GET` | `IsAuthenticated` | **الملف الشخصي:** جلب الملف الشخصي الكامل للمستخدم الحالي (يكتشف الدور تلقائياً). |

---

## 🛠️ II. إدارة البيانات الأساسية (CRUD Endpoints)

تتطلب جميع مسارات الإدارة التالية صلاحية **المدرس أو المشرف (`IsTeacherOrAdmin`)**.

| الكيان | المسار | الطريقة | الدور المطلوب | الاستخدام في الواجهة الأمامية |
| :---: | :---: | :---: | :---: | :---: |
| **الصف** (`Class`) | `/api/core/classes/` | `GET` | `Teacher/Admin` | عرض قائمة جميع الصفوف. |
| | `/api/core/classes/` | `POST` | `Teacher/Admin` | إنشاء صف جديد (مثال: 'الخامس أ'). |
| | `/api/core/classes/{id}/` | `PUT/DELETE` | `Teacher/Admin` | تحديث أو حذف صف محدد. |
| **المادة** (`Subject`) | `/api/core/subjects/` | `GET` | `Teacher/Admin` | عرض قائمة بجميع المواد الدراسية. |
| | `/api/core/subjects/` | `POST` | `Teacher/Admin` | إضافة مادة جديدة (مثال: 'الرياضيات'). |
| **الدورة** (`Course`) | `/api/core/courses/` | `GET` | `Teacher/Admin` | عرض جميع الدورات التدريبية المتاحة في النظام. |
| | `/api/core/courses/` | `POST` | `Teacher/Admin` | إنشاء دورة (ربط مادة بصف ومدرس). |

---

## 🎓 III. إدارة الواجبات والدرجات (Grading)

تتطلب جميع هذه المسارات صلاحية **المدرس أو المشرف (`IsTeacherOrAdmin`)** لإجراء التعديلات (POST/PUT/DELETE).

| الكيان | المسار | الطريقة | الدور المطلوب | الاستخدام في الواجهة الأمامية |
| :---: | :---: | :---: | :---: | :---: |
| **الواجب** (`Assignment`) | `/api/core/assignments/` | `GET` | `Teacher/Admin` | عرض قائمة بجميع الواجبات المضافة. |
| | `/api/core/assignments/` | `POST` | `Teacher/Admin` | إضافة واجب جديد لدورة محددة (`course_id`). |
| | `/api/core/assignments/{id}/` | `PUT/DELETE` | `Teacher/Admin` | تعديل أو حذف واجب محدد. |
| **الدرجة** (`Grade`) | `/api/core/grades/` | `POST` | `Teacher/Admin` | **إدخال الدرجة:** تسجيل درجة طالب (`student_id`) في واجب (`assignment_id`). |
| | `/api/core/grades/` | `GET` | `Teacher/Admin` | عرض جميع الدرجات المدخلة في النظام. |
| | `/api/core/grades/{id}/` | `PUT/DELETE` | `Teacher/Admin` | تعديل أو حذف درجة محددة. |